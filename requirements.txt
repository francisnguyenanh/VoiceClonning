# === VoiceClone Studio requirements ===
# Python 3.11 required (Coqui TTS does not support Python 3.12+)
# Install: python3.11 -m venv .venv && .venv/bin/pip install -r requirements.txt

# Web framework
flask>=3.0.0
flask-socketio>=5.3.6

# TTS & Voice Cloning (requires Python <3.12)
TTS==0.22.0

# llvmlite / numba — pre-built wheel needed on macOS without LLVM
llvmlite==0.43.0
numba==0.60.0

# Audio processing
librosa>=0.10.1
soundfile>=0.12.1
scipy>=1.11.0
noisereduce>=3.0.2
numpy>=1.24.0,<2.0

# Document parsing
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.2
pdfminer.six>=20221105

# Utilities
werkzeug>=3.0.0
